spring:
  application:
    name: eureka-service
  #    import: configserver:http://localhost:8888
  cloud:
    config:
      uri: http://localhost:8888
#  profiles:
#    active: prod

logging:
  level:
    web: debug
